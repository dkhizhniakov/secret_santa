# ===========================================
# Secret Santa - Server Environment
# ===========================================
# Скопируйте в server/.env и заполните значения

# Server
PORT=8080
ENV=development

# Database
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=secret_santa

# JWT (в продакшене: openssl rand -base64 32)
JWT_SECRET=dev-secret-change-in-production

# CORS
CORS_ORIGINS=http://localhost:3000

# 🎅 Secret Santa - План разработки

## ✅ Что уже сделано

### Инфраструктура

- ✅ AWS деплой (EC2 + RDS + S3 + CloudFront)
- ✅ Terraform для IaC
- ✅ GitHub Actions для CI/CD
- ✅ Docker для локальной разработки

### Backend

- ✅ Go + Gin + GORM
- ✅ PostgreSQL
- ✅ JWT авторизация
- ✅ Google OAuth
- ✅ Telegram Login
- ✅ S3 для аватаров
- ✅ Базовые CRUD розыгрышей
- ✅ Жеребьевка (без ограничений)

### Frontend

- ✅ React + TypeScript + Vite
- ✅ Material-UI
- ✅ TanStack Query
- ✅ react-hook-form + zod
- ✅ Страницы: Login, Dashboard, CreateRaffle, RaffleDetail, Profile
- ✅ Загрузка аватаров розыгрышей
- ✅ Профиль пользователя (адрес, wishlist, о себе)

---

## 📋 Текущие модели

### User

```go
type User struct {
    ID         uuid.UUID
    GoogleID   *string
    TelegramID *int64
    Name       string
    AvatarURL  *string
}
```

### Group (в процессе переименования → Raffle)

```go
type Group struct {
    ID          uuid.UUID
    Name        string
    Description string
    AvatarURL   *string
    InviteCode  string
    Budget      string      // "1000-3000 RUB"
    EventDate   *time.Time
    OwnerID     uuid.UUID
    IsDrawn     bool
    Members     []Member
}
```

---

## 🚧 Что нужно доработать

### ✅ Фаза 1: Профиль участника (ЗАВЕРШЕНО)

**Цель**: Расширить профиль пользователя

- ✅ Модель `UserProfile` (дефолтный профиль для переиспользования)

  - Адрес доставки (локальный + английский)
  - Телефон
  - О себе
  - Wishlist (что хочу)
  - Антивишлист (аллергии, что не дарить)

- ✅ API эндпоинты:

  - `GET /api/profile` - получить профиль
  - `PUT /api/profile` - обновить профиль

- ✅ UI страница `/profile` с формой (react-hook-form + zod)

### ✅ Фаза 2: Расширенное участие (ЗАВЕРШЕНО)

**Цель**: Профиль участника в конкретном розыгрыше

- ✅ Модель `Participant` (расширение `Member`)

  - Связь с `UserProfile` (автокопирование при вступлении)
  - Индивидуальные данные для розыгрыша
  - `GifteeID` - кому дарит (после жеребьевки)

- ✅ API эндпоинты:

  - `PUT /api/raffles/:id/my-profile` - обновить мой профиль в розыгрыше
  - `GET /api/raffles/:id/my-giftee` - информация о получателе (после жеребьевки)

- ✅ UI:
  - Диалог заполнения профиля при вступлении в розыгрыш
  - Страница `/raffle/:id/giftee` с полной информацией о получателе (wishlist, адрес, телефон)

### Фаза 3: Ограничения (Exclusions)

**Цель**: Создатель может указать кто не должен дарить кому

- [ ] Модель `Exclusion`

  ```go
  type Exclusion struct {
      RaffleID        uuid.UUID
      ParticipantA_ID uuid.UUID  // A не дарит B
      ParticipantB_ID uuid.UUID  // B не дарит A
      Reason          string
  }
  ```

- [ ] API эндпоинты:

  - `GET /api/raffles/:id/exclusions`
  - `POST /api/raffles/:id/exclusions`
  - `DELETE /api/raffles/:id/exclusions/:id`

- [ ] Обновить алгоритм жеребьевки с учётом ограничений

- [ ] UI: страница управления ограничениями (матрица или список пар)

### Фаза 4: Анонимный чат

**Цель**: Даритель и получатель могут общаться анонимно

- [ ] Модель `Message`

  ```go
  type Message struct {
      RaffleID   uuid.UUID
      SantaID    uuid.UUID   // Participant ID дарителя
      GifteeID   uuid.UUID   // Participant ID получателя
      FromSanta  bool        // true = от дарителя, false = от получателя
      Content    string
      ReadAt     *time.Time
  }
  ```

- [ ] WebSocket сервер (`gorilla/websocket`)

  - Endpoint: `WS /ws/chat`
  - Аутентификация через JWT
  - Broadcast сообщений в паре santa↔giftee

- [ ] API эндпоинты (история):

  - `GET /api/raffles/:id/chat/giftee` - чат с получателем
  - `GET /api/raffles/:id/chat/santa` - чат с дарителем

- [ ] UI:
  - Компонент реалтайм чата
  - Две вкладки: с получателем / с дарителем
  - Индикатор "печатает..."
  - Badge непрочитанных сообщений

---

## 🎯 Порядок реализации

### ✅ Спринт 1: Профиль (ЗАВЕРШЕНО)

1. ✅ Модель `UserProfile` + миграции
2. ✅ API для профиля (`GET /api/profile`, `PUT /api/profile`)
3. ✅ UI страница `/profile` с валидацией
4. ⏭️ Автозаполнение из профиля при вступлении в розыгрыш (для следующего спринта)

### ✅ Спринт 2: Расширенное участие (ЗАВЕРШЕНО)

1. ✅ Модель `Member` расширена полями профиля и `GifteeID`
2. ✅ Автокопирование профиля из `UserProfile` при join
3. ✅ Диалог `ParticipantProfileDialog` для заполнения профиля
4. ✅ Страница `GifteePage` с wishlist/адресом получателя
5. ✅ Обновлена жеребьевка для сохранения `GifteeID` в `Member`

### Спринт 3: Ограничения (2-3 дня)

1. Модель `Exclusion`
2. UI управления ограничениями
3. Обновить алгоритм жеребьевки
4. Валидация (достаточно ли участников)

### Спринт 4: Анонимный чат (4-5 дней)

1. WebSocket сервер
2. Модель `Message` + хранение в БД
3. UI компонент чата
4. Интеграция в страницу розыгрыша

### Спринт 5: Polish (1-2 дня)

1. Улучшения UI/UX
2. Мобильная адаптация
3. Тестирование

---

## 📊 Приоритеты

**Must have (для MVP):**

- Профиль участника с адресом/wishlist
- Страница "мой получатель" после жеребьевки
- Базовый анонимный чат

**Nice to have:**

- Ограничения (exclusions) - Спринт 3
- Реалтайм WebSocket - Спринт 4
- Email уведомления
- Индикатор "печатает..."

**Future:**

- Темная тема
- История розыгрышей (архив)
- Статистика

**✅ Реализовано:**

- ✅ Мультиязычность (ru/en/es) - i18next
